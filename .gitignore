venv/
.venv/
__pycache__/
*.pyc

# large raw datasets — download separately (see README)
dataset/
data/raw/
data/processed/

# editor / tooling config
.vscode/
.claude/
.ipynb_checkpoints/

# model weights & checkpoints
*.pth
*.save
checkpoint_ep*.pth
lstm_model.pth
lstm_scaler.save

# ...except the trained agent (80 KB) — deployments serve it, they don't train
!microgrid_dqn.pth

# generated outputs
microgrid_data.csv
microgrid_results.png
